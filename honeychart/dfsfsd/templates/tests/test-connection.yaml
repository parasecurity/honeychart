apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dfsfsd.fullname" . }}-test-connection"
  labels:
    {{- include "dfsfsd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dfsfsd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
