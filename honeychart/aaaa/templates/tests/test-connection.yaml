apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aaaa.fullname" . }}-test-connection"
  labels:
    {{- include "aaaa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aaaa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
