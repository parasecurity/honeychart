apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aaaa.fullname" . }}
  labels: {{- include "aaaa.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels: {{- include "aaaa.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations: {{- toYaml . | nindent 8 }}
      {{- end }}
      labels: {{- include "aaaa.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets: {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "aaaa.serviceAccountName" . }}
      securityContext: {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name:  cowrie
          securityContext: {{- toYaml .Values.securityContext | nindent 12 }}
          image:           "{{ .Values.honeypots.cowrie.image.repository }}:{{
          .Values.honeypots.cowrie.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.honeypots.cowrie.image.pullPolicy }}
          ports:
            - protocol: {{ .Values.honeypots.cowrie.ports.protocoltelnet}}
              containerPort: {{ .Values.honeypots.cowrie.ports.containerPorttelnet}}
              name:  {{ .Values.honeypots.cowrie.ports.nametelnet }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end  }}
      {{- with .Values.affinity }}
      affinity: {{- toYaml . | nindent 8 }}
      {{- end   }}
      {{- with .Values.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end    }}
